1. 해싱은 레코드의 키로부터 변환된 주소에 저장하는 과정(key to address transformation)으로 키가 있는 위치를 계산하여 바로 찾아가는 계산 탐색 방식이며, 해시 함수(hash function)가 필요하다.
2. 해시 함수는 데이터의 효율적 관리를 목적으로 임의의 길이의 데이터를 고정된 길이의 데이터로 매핑하는 함수이다.
3. 해싱에서 오버플로우는 완전히 채워진 버켓에 해싱되는 경우에 발생하며 오버플로우를 해결하는 방법에는 개방 주소 방법과 폐쇄 주소 방법이 있다.